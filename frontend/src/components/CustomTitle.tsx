function CustomTitle({ title }: { title: string }){
    return(
        <div className="py-2">
            <h1 className="text-white">{title}</h1>
        </div>
        
    )
}
export default CustomTitle